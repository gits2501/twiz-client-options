{
  "name": "twiz-client-options",
  "version": "1.0.0",
  "description": "options module for twiz-client",
  "main": "src/Options.js",
  "scripts": {
    "lint": "eslint src/Options",
    "tap": "tap -Rspec test/*.js ",
    "test": "nyc npm run tap && nyc report --reporter=text-lcov | coveralls"
  },
  "author": "github.com/gits2501",
  "license": "MIT",
  "dependencies": {
    "twiz-client-utils": "https://github.com/gits2501/twiz-client-utils.git"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "nyc": "^11.7.1",
    "tap": "^11.1.4"
  }
}
